link=document.links
arr=new Array(link)
var ls=[]
var len=0
arr.forEach(function name(params) {
for (let i=0;i<=params.length;i++){
    if (String(params[i]).includes("harry")){
        console.log(params[i])
    }
}
});